name: Linter
on: push

jobs:
  builds:
    runs-on: ubuntu-latest

    steps:
      - name: Clone repo
        uses: actions/checkout@v2

      - uses: actions/setup-java@v2
        with:
          java-version: 19

      - name: Run tests
        run: ./gradlew test